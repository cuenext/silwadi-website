name: Temporary Endodontics hero repair (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary Endodontics hero repair is disabled."
